Simple Project to provide a GraphQL API having subscriptions

Start the project via 

```
$ npm run start
```

Visit http://localhost:3000/graphql for the playground. Example Requests can be found in the `requests.http`